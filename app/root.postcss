@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
	:root {
		--header-h: 150px;
	}

	body {
		@apply bg-primary-text;
	}

	header {
		height: var(--header-h);
	}

	main {
		min-height: calc(100vh - var(--header-h));
	}
}
